#include<stdio.h>

#define MINLENGTH 81
int readbuf(char *buffer)
{
	int i = 0;
	int c;
	while(i < MINLENGTH)
	{
		c = getchar();
			
	}
}
int main()
{
	char buffer[MINLENGTH];
	while(EOF)
	{
		readbuf(buffer);
	}
}
